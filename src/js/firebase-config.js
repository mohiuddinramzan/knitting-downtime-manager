// Paste your Firebase project's Web config here to turn on multi-device
// real-time sync (see README section "Multi-device real-time sync").
//
// Leave apiKey as 'YOUR_API_KEY' to keep running in single-device mode
// (localStorage only) — the app works fine either way.
window.KDM_FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
};
